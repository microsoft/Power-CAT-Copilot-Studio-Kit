# M365 agent dev environment overrides for SetSail.ps1.
# APP_NAME_SUFFIX is appended to the Copilot name in M365 to tag dev builds
# (e.g. "Ask - Salesforce [v0.5]"). Leave blank for production.

APP_NAME_SUFFIX=
M365_TITLE_ID=
M365_APP_ID=
